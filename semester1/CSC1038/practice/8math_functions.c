#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    float x = 45;

    //x = sqrt(x); // square root of x
    //x = pow(x, 2); // x to the power of 2
    //x = round(x); //rounding x
    //x = ceil(x); // round up by using ceiling function
    //x = floor(x); // round down using floor function
    //x = abs(x); // absolute value of x (distance from 0)
    //x = log(x);

    //trigonometry functions
    //x = sin(x);
    //x = cos(x);
    x = tan(x);

    printf("%f\n", x);

    return 0;
}