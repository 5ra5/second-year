#include <stdio.h>

int main(){

    // arithmetic operators = + - * / % ++ --

    int x = 10;
    int y = 2;
    int z = 0;

    
    //z = x + y; // addition
    //z = x - y; // subtraction
    //z = x * y; // multiplication
    //z = x / y; // division
    //z = x % y; // remainder after divison - modulus
    
    //x++; // incrementation x + 1
    //x--; // decremenatation x - 1


    // AUGMENTED ASSIGNMENT OPERATORS
    //x = x + 2;
    //x += 2;

    //x = x - 3;
    //x -= 3;

    // x = x * 2;
    //x *= 2;

    //x = x / 2;
    //x /= 2;


    printf("%d\n", z);
    printf("%d", x);

    return 0;
}