## Background

Processes & operating system share memory
And... Program must be brought (from disk) into memory and placed within a process for it to be run
Main memory and registers are the only storage CPU can access directly
