Maintainer: Manage -> Members -> Invite members -> search for and select [@healygr](/healygr "Graham Healy") -> set Select Maximum role to maintainer -> click Invite; Private: Settings -> General -> Visibility, project features, permissions -> Project visibility = private.

1. Add your files relating to the manual/help command to the `manual` directory.
2. Your `makefile` should build the binary and place it in the `bin` directory.
3. For Stage3, you should only add a file named `video.txt` containing the link to your video submission. This folder should only contain this single file.